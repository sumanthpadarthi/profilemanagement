import React from 'react';
import { Container, Typography } from '@mui/material';

const NotFound = () => (
  <Container sx={{ mt: 4 }}>
    <Typography variant="h4" color="error">404 - Pages Not Found !</Typography>
  </Container>
);

export default NotFound;
